
description = 'Jasig CAS Client for Java - Tomcat 8.x Integration'
dependencies {
  compile project(':cas-client-integration-tomcat-common')
  compile project(':cas-client-support-saml')
  compile project(':cas-client-core')
    compile(group: 'org.apache.tomcat', name: 'tomcat-catalina', version:'8.0.1') {
exclude(module: 'tomcat-servlet-api')
exclude(module: 'tomcat-juli')
exclude(module: 'tomcat-annotations-api')
exclude(module: 'tomcat-api')
exclude(module: 'tomcat-util')
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
}
